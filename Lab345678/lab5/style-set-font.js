import { StyleSheet } from 'react-native';
export const styleSetFont = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        paddingHorizontal: 20,
    },
    content: {
        fontSize: 14,
        fontFamily: 'myFont',
    },
});
